apiVersion: v1
kind: Service
metadata:
  name: nodeport-service
  namespace: my-namespace

spec:
  type: NodePort
  selector:
    app: demoapp2

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30007