apiVersion: v1
kind: Pod
metadata:
  name: app3
  namespace: my-namespace
  labels:
    app: demoapp3

spec:
  containers:
    - name: nginx
      image: nginx:alpine
      ports:
      - containerPort: 80